// Envoy Rate Limit Service v3 API
// This file will contain the protobuf definitions for Envoy's rate limit service.
// For now, we'll download the official proto files from Envoy in a later step.

// The main service interface we need to implement:
// service RateLimitService {
//   rpc ShouldRateLimit(RateLimitRequest) returns (RateLimitResponse);
// }

// Proto files to be added:
// - envoy/service/ratelimit/v3/rls.proto
// - envoy/config/ratelimit/v3/rls.proto
// - envoy/type/v3/ratelimit.proto

// These will be downloaded from:
// https://github.com/envoyproxy/envoy/tree/main/api
